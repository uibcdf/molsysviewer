[build-system]
requires = ["setuptools>=64", "versioningit>=3", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "molsysviewer"
dynamic = ["version"]
description = "Mol*-powered interactive molecular viewer for Jupyter with reproducible scene state"
authors = [{name = "UIBCDF"}]
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
  "aiohttp>=3.10",
  "anywidget>=0.9.15",
  "argdigest>=0.12.1",
  "depdigest",
  "molsysmt>=0.22.0",
  "numpy",
  "packaging>=23",
  "pyunitwizard",
  "smonitor>=0.13.0",
  "traitlets>=5",
]
license = "MIT"
keywords = [
  "molecular visualization",
  "molecular viewer",
  "Jupyter",
  "Mol*",
  "structural biology",
  "cheminformatics",
  "MolSysMT",
  "anywidget",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
  "Topic :: Scientific/Engineering :: Chemistry",
  "Topic :: Scientific/Engineering :: Visualization",
]

[project.urls]
Homepage = "https://github.com/uibcdf/molsysviewer"
Documentation = "https://www.uibcdf.org/molsysviewer"
Issues = "https://github.com/uibcdf/molsysviewer/issues"

[project.optional-dependencies]
dev = [
  "build",
  "pyyaml",
  "ruff==0.16.5",
  "pytest",
  "pytest-cov",
]
# YAML support for user presets (msv.config.load_user_presets with .yaml files)
yaml = ["pyyaml"]
# Movie export (view.movie.export)
movie = ["imageio"]
# The standalone Qt prototype requires PySide6_uibcdf with QtWebEngineWidgets.
# This is NOT installable via pip — use the UIBCDF conda channel:
#   conda install -c uibcdf -c conda-forge pyside6-addons-uibcdf
standalone = []

[project.scripts]
molsysviewer = "molsysviewer.standalone:main"
molsysviewer-qt = "molsysviewer.standalone_qt:main"
molsysviewer-server = "molsysviewer.remote.server_cli:main"

[tool.setuptools]
include-package-data = false

[tool.setuptools.packages.find]
namespaces = true
include = ["molsysviewer", "molsysviewer.*"]
exclude = ["molsysviewer.js", "molsysviewer.js.*"]

[tool.setuptools.package-data]
"molsysviewer" = ["remote_protocol.json", "runtime_actions.json", "viewer.js", "VARELA_ROUND_OFL.txt"]

[tool.ruff]
line-length = 120
target-version = "py311"
include = ["*.py", "*.pyi"]
# Directories to analyze (adjust if needed).
src = ["molsysviewer", "tests"]
extend-exclude = [
    "ACKREDIT_GUIDE.md",
    "ARGDIGEST_GUIDE.md",
    "DEPDIGEST_GUIDE.md",
    "devtools/quarantine",
    "GH_RUN_RECEPTOR_GUIDE.md",
    "MOLSYSSUITE_GUIDE.md",
    "PYTEST_RECEPTOR_GUIDE.md",
    "PYUNITWIZARD_GUIDE.md",
    "SMONITOR_GUIDE.md",
]

[tool.ruff.lint]
# Base rules (similar to flake8 + import order, etc.)
select = ["E4", "E7", "E9", "F", "I", "B006", "B023"]
ignore = [
  "E203",  # keep compatibility with the existing formatted code around slices
]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]
"devtools/benchmarks/endpoint_isolation.py" = ["E402"]
"devtools/benchmarks/scene_history_memory.py" = ["E402"]
"molsysviewer/js/tests/e2e/*-bridge.py" = ["E402"]
"molsysviewer/config/__init__.py" = ["E402"]
"tests/integration/test_molsysmt_integration.py" = ["E402"]
"tests/test_addons.py" = ["E402"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

[tool.versioningit]
default-version = "0+unknown"

[tool.versioningit.format]
distance = "{base_version}+{distance}.{vcs}{rev}"
dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"
distance-dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"

[tool.versioningit.vcs]
# The method key:
method = "git"  # <- The method name
# Parameters to pass to the method:
match = ["*"]
default-tag = "0.0.0"

[tool.versioningit.tag2version]
regex = '^(?P<version>(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*))$'
require-match = true

[tool.versioningit.write]
file = "molsysviewer/_version.py"
