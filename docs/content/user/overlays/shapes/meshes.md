# Mesh primitives

Use mesh primitives to render custom geometry:

- triangle faces
- tetrahedra

This page is a placeholder. It will include minimal examples for:

- `view.shapes.add_triangle_faces(...)`
- `view.shapes.add_tetrahedra(...)`

