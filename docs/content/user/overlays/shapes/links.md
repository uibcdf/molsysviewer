# Links (networks)

Links render connections between points (for example, interaction networks).

This page is a placeholder. It will include a minimal example for `view.shapes.add_links(...)` and the key options.

