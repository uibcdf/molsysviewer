# Displacement vectors

Displacement vectors render arrows (or segments) to visualize directions and magnitudes.

This page is a placeholder. It will include a minimal example for `view.shapes.add_displacement_vectors(...)` and the key options.

