# Docs embeds (HTML lite)

This page is a placeholder.

It will include:

- Common path issues with `_static/views/*.html`.
- When you can open `_build/html/index.html` directly in Chrome vs when you need to serve the docs.
- What to check when an iframe stays blank.

