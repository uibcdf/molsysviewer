# Navigation between structures

Many molecular systems include multiple **structures**.

- If you loaded a molecular dynamics trajectory, these structures usually correspond to trajectory frames.
- If you loaded an ensemble or an NMR model set, they correspond to models (not necessarily time-ordered).

The on-canvas controls can expose a structure navigation bar when multiple structures are available.

