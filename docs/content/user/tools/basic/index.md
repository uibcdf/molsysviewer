# Basic tools

`molsysviewer.tools.basic` contains broadly useful composition helpers.

These functions are **pure**: they return a new `MolSysView` instead of mutating one of the input views.

```{toctree}
:maxdepth: 1

concatenate_structures.md
merge_views.md
```
