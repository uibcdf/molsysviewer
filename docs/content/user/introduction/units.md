# Units (PyUnitWizard)

Some MolSysViewer arguments accept values with units (for example, camera durations or distances).

This page is a placeholder.

It will include:
- how to pass values as plain numbers (assumed units),
- how to pass values with units, and
- common unit pitfalls when mixing Å, nm, ms, and seconds.

